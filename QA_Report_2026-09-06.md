# تقرير فحص شامل - نظام مركز الدروس (EtuCenter)

**التاريخ:** 2026-09-06
**البيئة:** نسخة محلية (PostgreSQL محلي `gestion_centre`) + بناء إنتاجي Next.js 16.2.12
**النتيجة العامة (المرحلة 1):** ✅ **38/38 فحصًا ناجحًا** + **ملاحظة واحدة (Finding)** يجب مراجعتها
**النتيجة العامة (المرحلة 2 - S1–S18):** ✅ **234/234 فحصًا ناجحًا** + **9 ملاحظات** (7 مشاكل حقيقية، 1 تصميم، 1 سلوك موثّق)

---

## 1) المنهجية

- تمت التجربة **كليًا على نسخة محلية** باستخدام مركز تجريبي **مؤقت** (`QA-Zab-<timestamp>`) بكامل بياناته:
  - مدير (`admin`)، أستاذ (`prof`)، تلميذان (`eleveForfait` بشراكة خاصة، `eleveNormal` بدون شراكة).
  - كلمة مرور موحدة `QaZab@2026`، بريد وهمي `@test.local`.
- بعد اكتمال الفحص، **يُحذف المركز التجريبي تلقائيًا** (لا يُترك أي أثر في قاعدة البيانات).
- **لم يتم أي تعديل على قواعد بيانات `Ghodhbeni` ولا `olfa_center` إطلاقًا** — بقيت بمحتواها الأصلي.
- الفحص يجمع بين: استدعاءات HTTP حقيقية (تسجيل دخول، نقاط الواجهة) + تحقق مباشر من قاعدة البيانات (prisma).
- أداة الفحص: سكربت ذاتي (seed → 38 فحصًا → teardown).

---

## 2) نتائج الفحوصات

| # | الفحص | النتيجة |
|---|-------|---------|
| 1 | تسجيل دخول المدير (NextAuth credentials) | ✅ |
| 2 | إنشاء مادة | ✅ |
| 3 | إنشاء مجموعة بالأقساط (110 / 5) → سعر 22 | ✅ |
| 4 | إنشاء مجموعة بسعر ثابت 12.5 | ✅ |
| 5 | تسجيل `eleveForfait` في مجموعة الأقساط | ✅ |
| 6 | تسجيل `eleveNormal` في مجموعة الأقساط | ✅ |
| 7 | تسجيل `eleveNormal` في المجموعة الثابتة | ✅ |
| 8 | شراكة خاصة للتلميذ (150 / 20) → 7.5 (تحقق DB) | ✅ |
| 9 | تسجيل مكرر → خطأ 409 | ✅ |
| 10 | تسجيل دخول الأستاذ | ✅ |
| 11 | RBAC: الأستاذ محظور من API المدير (401) | ✅ |
| 12 | إنشاء جلسة (حصة) اليوم بحالة "مجدولة" | ✅ |
| 13 | تسجيل حضور في جلسة اليوم | ✅ |
| 14 | **الخصم الفوري** بمبلغ الشراكة 7.5 (وليس سعر المجموعة 22) | ✅ |
| 15 | الحصة تبقى "مجدولة" (دليل أن الخصم تم قبل "منتهية") | ✅ |
| 16 | الديون (DB) = 7.5 | ✅ |
| 17 | صافي الرصيد = -7.5 | ✅ |
| 18 | التلميذ العادي في مجموعة الأقساط يُحاسب على 22 | ✅ |
| 19 | انقلاب حضور→غياب: الاستهلاك أصبح reversed | ✅ |
| 20 | انقلاب حضور→غياب: الدين = 0 فورًا | ✅ |
| 21 | مبلغ الإلغاء (reversal) = 7.5 | ✅ |
| 22 | إنشاء حصة تعويضية (ماضي) | ✅ |
| 23 | التعويضية: "منتهية" + استهلاك 7.5 (وليس 22) | ✅ |
| 24 | دين التعويضية = 7.5 | ✅ |
| 25 | تعويضية مكررة (نفس التاريخ) → 409 | ✅ |
| 26 | تعديل شراكة `eleveNormal` إلى 160/20 (DB) | ✅ |
| 27 | تعديل سعر المجموعة الثابتة 12.5 → 20 | ✅ |
| 28 | تعديل أقساط المجموعة 200/10 → سعر 20 | ✅ |
| 29 | دفع 10 دينار | ✅ |
| 30 | المتبقي = max(0, دين − مدفوع) = 0 | ✅ |
| 31 | تفاصيل المجموعة (مدير): إجمالي الدين 15.5 (7.5+8) | ✅ |
| 32 | قائمة التلاميذ (مدير): إجمالي الدين 15.5 | ✅ |
| 33 | عزلة المراكز: مجموعة مركز آخر → 404 | ✅ |
| 34 | عزل المواد: مواد المركز نفسه فقط | ✅ |
| 35 | **إعادة التسعير الذاتي**: استهلاك 22 → 8 عند إعادة الحضور | ✅ |
| 36 | تسجيل إعادة التسعير في SystemLog | ✅ |
| 37 | عرض التلميذ "مدفوعاتي" (يعيد المجموعات والأداء) | ✅ |
| 38 | RBAC: التلميذ محظور من API المدير (401) | ✅ |

---

## 3) الملاحظة الجوهرية (Finding) ⚠️

### انقلاب حضور → غياب يُحدث رصيدًا دائنًا (active reversal) بتوازن خاطئ

**السيناريو:** تلميذ لم يدفع أي شيء، حضره ثم حُوّل إلى "غياب".

| الحالة | الدين | صافي الرصيد |
|--------|-------|-------------|
| بعد الحضور | 7.5 | -7.5 |
| بعد الانقلاب (المتوقع) | 0 | **0** |
| بعد الانقلاب (الفعلي) | 0 | **+7.5** |

**المشكلة:** الدالة `reverseCourseAttendance` في `src/lib/student-finance.ts` (حوالي السطر 414-477):
- تحوّل الاستهلاك الأصلي إلى `reversed` (جيد) ✅
- **وتُنشئ بالإضافة** سطر إلغاء (reversal) نشطًا بقيمة `+7.5` ✅

**لكن** هذا التلميذ لم يدفع شيئًا من الأساس — الاستهلاك الأصلي سلبي (`-7.5`). سطر الإلغاء **يجب أن يعوّض الاستهلاك** (أي يجعل الناتج صفرًا: يضيف `+7.5` ليُبطل `-7.5`)، وليس أن يُقيد رصيدًا دائنًا جديدًا. الناتج النهائي: **رصيد دائن وهمي +7.5 لا يقابله أي مال** — يظهر كأن المركز مدين للتلميذ.

**الوصية المعقولة للتصحيح:** في حالة الحضور المبكر (الخصم قبل إنهاء الحصة)، يجب **حذف/إلغاء الاستهلاك المقتطع فورًا** دون إنشاء استهلاك تعويضي دائم — أو إنشاء إلغاء يحمل قيمة موجبة **وعلامة "يُبطل استهلاك X"** لا تُحسب ضمن الرصيد. الخيار الموازي: إعادة المؤشرات لتصفّر (debit+credit) بدون ترحيل سطر دائن للرصيد الصافي.

---

## 4) ملاحظات محلية (ليست مشاكل في الإنتاج)

1. **قرينة اسم الكوكيز:** الكود يفرض أسماء `__Secure-`/`__Host-` عندما `NODE_ENV=production` (`src/lib/auth.ts` السطور 167-195)، بينما الوسيط وسط `next-auth` يشتق الاسم من `NEXTAUTH_URL`/`VERCEL`. محليًا تم التشغيل بـ `NEXTAUTH_URL=https://localhost:3000` لمطابقة السلوك. **في الإنتاج (Vercel/HTTPS) لا تأثير لذلك.**
2. **حد الطلبات (rate limit) في NextAuth:** ~5 دخولات credential في جلسة واحدة قد تسبب 429 مؤقتًا — سكربت الفحص يعيد المحاولة (backoff).
3. **الإصلاح الذي حدث في السكربت:** سبب فشل الفحص لم يكن في التطبيق، بل خطأ في السكربت نفسه (تم إرسال UUID كبريد للتسجيل). بعد التصحيح: فحص التلميذ ناجح ويعيد `{groupes, paiements}` بشكل صحيح (مثال: `groupes[0] = {groupe, totalPaid: 10, unpaid: 0}`).

---

## 5) حالة الإنتاج

- **لم تُلمس `Ghodhbeni` ولا `olfa_center`.** كل ما سبق تم في نسخة محلية.
- السلوكيات المثبتة (الخصم الفوري، إعادة التسعير الذاتي، إلخ) **مطابقة** لنسخة الإنتاج الحالية (نفس الشيفرة/نفس المنهجية المطبقة في قاعدة الإنتاج عبر أدوات التشغيل الآلي).
- أي قرار بتعديل الإنتاج (مثل تصحيح ملاحظة الرصيد الدائن) يجب أن يمر بمراجعة وموافقة مستخدمة أولًا.

---

## 6) خطوات مقترحة

1. مراجعة **ملاحظة الرصيد الدائن (3)** واتخاذ قرار: تعديل `reverseCourseAttendance` في النسخة المحلية واختبارها مجددًا، ثم دفعها للإنتاج.
2. تشغيل السكربت نفسه على نسخة الإنتاج (إشعاع مماثل) بعد الموافقة للتحقق النهائي.
3. أرشفة أداة الفحص في المستودع (GitHub Action / Jenkins) إن رغبت في اختبارات تجريبية دائمة.

---

# المرحلة الثانية: الفحص الموسّع (S1–S18)

## 7) المنهجية

- نطاق أوسع عبر الأدوار الأربعة: مدير (`admin`) وأستاذ (`profA`) وتلميذان (`eleve1F` بشراكة 110/5، `eleve2N` عادي) ومشرف عام (`super_admin`) في مركز مؤقت جديد `qa-zab2-*`.
- 234 فحصًا مع تحقق مباشر من قاعدة البيانات (prisma) لكل عملية مالية، وفحوصات **عزل (isolation)** عبر المراكز، وRBAC صارم لكل دور.
- في النهاية يُحذف المركز التجريبي كاملًا (teardown ✓ — لا أثر في قاعدة البيانات).
- فرق **مزامنة بين الأدوار**: admin == prof == eleve لـ: أعضاء المجموعة، الديون (قسم 20/80)، سجل الحضور، الرصيد، المطالبات (claimable)، وصافي الديون (impayeNet).

| # | المجموعة | مضمونها | النتيجة |
|---|---------|---------|---------|
| S1 | المصادقة وRBAC | دخول 4 أدوار، حظر تبادلي للطرق المخصصة (401/404) | ✅ |
| S2 | المواد | إنشاء/تعديل/حذف، عزل مواد المركز | ✅ |
| S3 | المجموعات والشراكات | إنشاء، تسعير بقسط/ثابت، سعة قصوى | ✅ |
| S4 | إدارة المستخدمين (مدير) | إنشاء/تعديل/أرشفة، تفرد البريد | ✅ |
| S5 | إدارة التلاميذ (أستاذ) | تسجيل/إلغاء/إعادة تسجيل/تفعيل مؤرشف | ✅ |
| S6 | لوحة التلميذ | مجموعات، ديون، رصيد | ✅ |
| S7 | الحصص والغيابات | إنشاء حصة/تعويضية، إنهاء، متوسط | ✅ |
| S8 | الحضور | تسجيل/غ/غياب/انقلاب، عدادات | ✅ |
| S9 | اعتمادات وخصومات التلميذ | netBalance/رصيد دائن، دقيق DB | ✅ |
| S10 | تمويل الأستاذ | رصيد 52==52، مطالبات، صافي | ✅ |
| S11 | تمويل التلميذ (مدير+تلميذ) | summary=45==45، اعتمادية idempotency | ✅ |
| S12 | الإشعارات والنشاط | إنشاء/قراءة، تخصيص الأدوار | ✅ |
| S13 | سجل النظام | تسجيل إعادة التسعير والأنشطة | ✅ |
| S14 | التسجيل العام والإعدادات | register + معلومات، تصحيح إعدادات | ✅ |
| S15 | المشرف العام | مراكز، إحصائيات، اشتراكات، أدوار | ✅ |
| S16 | النسخ الاحتياطي والاستعادة | تصدير/تحقق/تنزيل/استعادة/تسليم | ✅ |
| S17 | الأمان | middleware، عزل المراكز، تصديق المدخلات، حدود | ✅ |
| S18 | مزامنة الأدوار | موازين متطابقة عبر كل الشاشات مع DB | ✅ |

---

## 8) ملاحظات المرحلة الثانية

| # | المكون | الملاحظة | النوع/الأثر |
|---|--------|----------|-------------|
| 1 | إنشاء مادة (S2) | مادة مكوّنة من مسافات فقط تُقبل (201) — المخطط لا يقوم بـ `trim` على الاسم | مشكلة / منخفضة |
| 2 | حدود السعة (S5) | **إعادة تفعيل** تسجيل مؤرشف تتجاوز `capaciteMax` للمجموعة (إعادة الحالة النشطة لا تفحص السعة) | مشكلة / متوسطة |
| 3 | الدفع المسبق (S11) | **كسر التزامن idempotency**: `processStudentPayment` يستبدل مفتاح الطلب بـ `paiement:<newId>` (src/lib/payments.ts ~61-73) → إرسال نفس المفتاح مرتين يُنشئ دفعة ثانية + رصيد مسبق ثاني + دخل أستاذ مكرر (+40) | مشكلة / **عالية** |
| 4 | الاشتراكات (S15) | مدة الاشتراك غير مُصادق عليها: أي سلسلة (`week`) تُقبل → اشتراك نشط بتاريخ انتهاء = الآن (لا تحقق enum) | مشكلة / متوسطة |
| 5 | حذف دائم (S15) | المسلك غير قابل للوصول عمليًا: الوسيط يرفض 401 قبل التحقق من دور `super_admin` على `/api/admin/*` رغم وجود الفحص داخل المسلك | مشكلة / منخفضة |
| 6 | استعادة نسخة بلا ملف (S16) | 500 TypeError بدل 400 (`formData()` يرمي استثناء على `Content-Type` غير مدعوم قبل فحص وجود الملف) | مشكلة / متوسطة |
| 7 | استعادة نسخة عامة (S16) | **الاستعادة العامة معطّلة**: 500 `PrismaClientKnownRequestError` تعارض فريد على `id` عند `prisma.seance.create()` — إعادة النسخة تصطدم بصفوف موجودة بنفس المعرّفات (لا دمج بالمعرّفات الطبيعية للحصص) | مشكلة / **عالية** |
| 8 | حصص التلميذ (S18) | قائمة الحصص تشمل الحصص السابقة (التعويضية من أمس) — لا فلتر ماضٍ/مستقبل | تصميم / منخفضة |
| 9 | إعدادات النظام (S14) | مفتاح غير معروف وحده → 400 «لا يوجد إعداد صالح»؛ مع مفتاح صالح → يُتجاهل بصمت | سلوك موثّق (ليس خطأ) |

### سلوكيات متحقق منها (إيجابية جديرة بالتوثيق)

- **استعادة مركزية دمجية = متطابقة (idempotent)**: إعادة رفع نفس النسخة لا تكرر الدفعات/الرصيد/دخل الأستاذ (تُتخطى الدفعات والمعاملات بنفس المعرّفات الطبيعية).
- **مزامنة الأدوار المالية كاملة**: رصيد المطالبات والحساب النهائي للأستاذ متطابق بين الأستاذ والمدير؛ دين المجموعة (35) = مجموع ديون التلاميذ عند الأستاذ؛ سجل الحضور متطابق.
- **فرق الرصيدين موثق**: رصيد التلميذ في التفاصيل = الرصيد الدائن (65) بينما ملخص `summary` = الصافي النشط للاستهلاك (45) — متراتبان بمقاييس مختلفة عن قصد.
- حساب رصيد الأستاذ في `summary` = مجموع علامات كل المعاملات (بلا فلتر حالة)؛ العكس صحيح في ملخص التلميذ (نشط فقط) — كليهما مطابق لقاعدة البيانات (52/52 و45/45).

---

## 9) الخلاصة

- **المرحلة الثانية: 234/234 ناجحة** — التطبيق متين سلوكيًا في الطبقات الأساسية (الأدوار، الحسابات، العزل، المزامنة عبر الأدوار).
- الأولوية للتصحيح: **(3) كسر idempotency الدفع المسبق** و**(7) استعادة النسخ العامة** (تعطّل فعلي)، ثم **(2) سعة المجموعات** و**(4) الاشتراكات** و**(6) استعادة بلا ملف**.
- لا توجد أي ملاحظة تمس **الأداء أو الأمن عبر المراكز** (العزل محكم 404/401 مطبّق).
- كل شيء في نسخة محلية؛ بيانات الإنتاج لم تُلمس.